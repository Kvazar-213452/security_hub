[package]
name = "security_hub"
version = "0.1.0"
edition = "2021"

[profile.release]
panic = "abort"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
warp = "0.3"
tokio = { version = "1", features = ["full"] }
